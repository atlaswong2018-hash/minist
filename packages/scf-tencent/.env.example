# minist SCF 环境变量示例
# 复制为 .env(仅本地开发用)或在腾讯云 SCF 控制台「函数管理 → 函数配置 → 环境变量」逐条填入。
# SCF 运行时会自动注入 TENCENTCLOUD_SECRETID/SECRETKEY/SESSIONTOKEN/FUNCTIONNAME/NAMESPACE/REGION,
# 这些不需要也不应该在这里配置。

# 管理员 Token(方案二:Token 自改超时配置鉴权)。
# 部署后用此 Token 调用 /api/admin/set-timeout 锁定防爆产配置。
# 建议用强随机字符串:openssl rand -hex 32
ADMIN_TOKEN=

# COS 桶名(用户自己的桶,用于同步备份与人物卡直传)。
# 命名规则:<自定义名>-<APPID>,例如 minist-backup-1250000000
COS_BUCKET=

# COS 地域。默认广州(华南)。
COS_REGION=ap-guangzhou

# LLM 上游地址(OpenAI 兼容)。默认 DeepSeek。
# Authorization 头由前端透传,这里只决定中转目标。
LLM_PROXY_URL=https://api.deepseek.com

# 可选:本地开发监听端口(SCF Web 函数固定 9000,本机可用 PORT 覆盖)。
# PORT=9000
