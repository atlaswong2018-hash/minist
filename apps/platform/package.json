{
  "name": "@minist/platform",
  "version": "0.1.0",
  "private": true,
  "description": "minist 案例部署平台:引导用户一键把 minist 酒馆部署到自己的 CF / 腾讯云账号,管理配置,同步角色卡,AI 排障。",
  "license": "AGPL-3.0-only",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -p tsconfig.json --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "vue-tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "vue": "^3.5.13",
    "@minist/shared": "*",
    "@minist/deploy-agent": "*"
  },
  "devDependencies": {
    "vite": "^8.1.0",
    "@vitejs/plugin-vue": "^6.0.7",
    "typescript": "~6.0.3",
    "vue-tsc": "^2.1.10"
  }
}
